import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    card: {
        width: "100%",
        height: 90,
        marginBottom: 5,
        borderRadius: 10
    },
    cardTitle: {
        color: "#fff",
        fontSize: 16,
        margin: 6
    }
});
